/**
 * Este arquivo é um exemplo de estrutura de comando inválida
 * Ele será detectado como INVÁLIDO porque não possui as propriedades obrigatórias
 * (data e execute) - veja em src/utils/loaders.js
 *
 * Descomente para usar, ou delete este arquivo
 */

// INVÁLIDO - Faltam propriedades
module.exports = {
  name: "invalido",
  // description: "...",
  // execute: async (interaction) => { }
};
